provisioner: existing-cluster

cluster_name: <cluster_name>

loadgenerators:
  - private_ip: <private_ip>
    public_ip: <public_ip>
    user: <ssh_username>

nodes:
  -  <hazelcast_member_ip>
  -  <hazelcast_member_ip>
  -  <hazelcast_member_ip>