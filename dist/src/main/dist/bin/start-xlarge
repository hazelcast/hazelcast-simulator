#!/bin/sh

if [ -z "${STABILIZER_HOME}" ] ; then
    export STABILIZER_HOME=$(cd $(dirname $(readlink -f $0 2> /dev/null || readlink $0 2> /dev/null || echo $0))/.. && pwd)
fi

echo  STABILIZER_HOME = ${STABILIZER_HOME}

$STABILIZER_HOME/bin/start-machines 25
